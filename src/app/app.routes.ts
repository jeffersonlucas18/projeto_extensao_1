import { Routes } from '@angular/router';
import { DashboardResultComponent } from './dashboard/dashboard-result/dashboard-result.component';

export const routes: Routes = [
  { path: 'dashboard', component: DashboardResultComponent },
];
